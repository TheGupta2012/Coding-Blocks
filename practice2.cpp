#include<bits/stdc++.h>
using namespace std;
typedef long long int lli;
lli mod = 1e9 + 7;
int main()
{
    lli t;
    cin>>t;
    while(t--)
    {
        lli n;
        lli ans = 0;
        cin>>n;
        lli x[n];
        vetcor < pair<lli,lli> > xb;
        xb.reserve(n+1);
        for(int i=0;i<n;i++)
        {
            cin>>x[i];
        }
        for(int i=0;i<n;i++)
        {
            cin>>b[i];
            xb.push_back({x[i],b[i]});
        }
        for(auto i:xb)
        {
            total_dist =
        }
        sort(xb.begin(),xb.end());

    }
    return 0;
}
